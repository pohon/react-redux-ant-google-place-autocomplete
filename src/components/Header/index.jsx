import { Layout } from "antd";
import './styles.css';

const { Header: AntdHeader } = Layout

const Header = () => {

  return (
    <AntdHeader>
      ini header
    </AntdHeader>
  )
};

export default Header;